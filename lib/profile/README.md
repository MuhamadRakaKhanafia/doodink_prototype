# Profile (UI/Branding) — DoodInk

Catatan: folder ini dibuat sebagai placeholder untuk implementasi UI profil/branding.

## Implementasi yang disarankan
- Splash screen (logo + loading anim)
- Main menu (nama/username + create/join)
- ThemeData global (gradient + warna fun)

## Struktur file
- `lib/app/theme/doodink_theme.dart` (ThemeData, color palette)
- `lib/features/main_menu/main_menu_screen.dart`
- `lib/features/splash/splash_screen.dart`

## Next step
Ketika Anda setuju, saya akan langsung scaffold:
- screen/splash
- screen/main menu
- theme + reusable widgets

